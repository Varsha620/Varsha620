<p class="type">Hello There,<br>Myself Varsha Sabu</p>

<style>
@keyframes typing { from { width: 0 } to { width: 31ch } }
@keyframes blink  { 50% { border-color: transparent } }

.type{
  font: 700 28px/1.35 "Fira Code", ui-monospace, Menlo, Consolas, monospace;
  color: #7c3aed;                    /* purple */
  width: 31ch;                       /* characters to reveal */
  overflow: hidden;
  white-space: nowrap;
  border-right: 2px solid #7c3aed;   /* caret */
  animation:
    typing 3.2s steps(31, end) 0.2s both,
    blink 1s step-end infinite;
}
</style>
